# Secret racine — ne JAMAIS commiter (private.yml et vault.yml sont
# committés CHIFFRÉS par ansible-vault avec ce mot de passe)
.vault-pass
*.pem
*.key

# Ansible
*.retry
.ansible/

# Divers
.DS_Store
